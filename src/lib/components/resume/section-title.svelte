<p class="section-title"><slot /></p>

<style lang="scss">
	.section-title {
		font-size: 24px;
		margin: 0;
		margin-bottom: 16px;
		border-bottom: 2px solid var(--primary-color);
		font-family: var(--title-font);
		font-weight: 600;
	}
</style>
